# 面试准备清单 — AI应用工程师

> 目标岗位：AI应用工程师 / Agent开发工程师 / AI自动化工程师
> 准备周期：1周
> 核心策略：用你的 Jarvis 生态当案例库，6个项目每个都能讲透

---

## 一、自我介绍（30秒版）

```
你好，我叫王文斌，信阳职业技术学院计算机应用技术专业，2026届应届生。

过去三个月我独立构建了一套多 Agent LLM 应用系统，从协议设计到服务治理
全链路落地。主要包括：MCP 工具链集成、跨平台通信桥接（飞书/Telegram）、
桌面自动化、本地 RAG 检索和三层服务治理架构。

GitHub 上有完整的项目文档：github.com/deer-cc/ai-agent-ecosystem

我对 AI Agent 的工程落地有实践经验，也关注企业级场景的可用性设计，
希望能加入贵团队做 AI 应用开发相关的工作。
```

---

## 二、项目讲解模板（每个项目按这个结构讲）

### 结构：背景 → 挑战 → 方案 → 结果 → 反思

#### 项目1：Multi-Agent 编排系统

**背景：** 单 Agent 长对话质量衰减，工具调用边界模糊，缺乏领域专业化

**挑战：** 如何在不增加上下文窗口的情况下，让多个"专家"协同工作？

**方案：**
- 叶子（规划层）只输出 JSON 指令，不直接调用工具
- 小天（执行层）解析指令、调用工具、回执结果
- 监护系统兜底：超时 30s 或连续 100 条无回执 → SIGTERM
- 三级安全边界防止误操作

**结果：** 7 个项目追踪、8 条决策记录、5 种沟通模式识别

**反思（面试官最爱问）：**
> "后来我发现叶子有时候分析过度——用户只是想快速执行一个简单任务，
> 叶子却花了 3 轮才确认方案。所以后来加了快速路径：简单问题直接跳过叶子
> 由小天自主决策，只有复杂问题才走完整规划流程。"

---

#### 项目2：cc-connect 消息桥接

**背景：** Claude Code 只能在终端运行，用户想在手机上通过飞书使用

**挑战：** 飞书凭证只能一个会话独享，多进程会互相踢

**方案：**
- Go 进程做持久化网关，维持单会话
- WebSocket 长连接 + HTTP 主动推送双通道
- 消息路由根据来源标记 session key，隔离不同会话

**结果：** 延迟 <200ms，99%+ 可用性，解决了凭证竞态问题

**反思：**
> "最初我用 HTTP polling，延迟高且浪费资源。后来改成 WebSocket 长连接
> 才解决实时性问题。这个经验让我理解了长连接 vs 短连接在不同场景下的
> 取舍。"

---

#### 项目3：桌面自动化 v2

**背景：** 原版用 PowerShell 管道调用 pyautogui，中文输入乱码，延迟 500ms+

**挑战：** 性能瓶颈在哪里？怎么优化？

**方案：**
- 去掉 PowerShell 中间层，Python 原生调用 Win32 API
- 原生 UTF-8 编码，解决 GBK 乱码
- HTTP API 封装，10 个端点覆盖常见操作

**结果：** 延迟 500ms → 50ms（10 倍提升）

**反思：**
> "这个项目的价值不在于用了什么新技术，而在于我学会了'测量→定位瓶颈→验证'
> 的工程思维。先加计时器定位哪个环节慢，再针对性优化，而不是瞎猜。"

---

#### 项目4：本地 RAG 检索

**背景：** 不想依赖外部 API 做知识库搜索，要完全离线

**挑战：** 没有向量数据库，怎么用纯 Python 实现检索？

**方案：**
- 倒排索引：Markdown 标题 → 文件路径映射
- H1-H4 层级权重：一级标题权重最高，四级最低
- 意图识别矩阵：4 类查询自动路由到不同索引策略

**结果：** 延迟 <100ms，74 个知识节点，零 API 依赖

**反思：**
> "我最初想直接用 Embedding + 向量相似度搜索，但发现对中文效果不稳定，
> 而且需要引入额外依赖。后来简化为倒排索引+关键词匹配，反而更稳定可靠。
> 这就是'够用就好'的工程哲学。"

---

#### 项目5：服务治理

**背景：** 手动管理 5 个服务，重启后全部挂掉，告警靠人盯着

**挑战：** 怎么让系统自己"活"着？

**方案：**
- guardian：每 60s 巡检，3 次失败 → 重启
- dispatcher：消息去重（5min 窗口）+ 指数退避重试（5s/15s/30s）+ 本地队列
- watchdog：进程存活 + 文件大小监控

**结果：** 5 个服务全自动守护，重启补发零丢失

**反思：**
> "最难的其实不是写守护逻辑，而是定义'健康'的标准。端口通不等于服务正常，
> 我后来加了业务级的健康检查——dispatcher 不仅检查端口，还测试 /api/health
> 返回内容是否符合预期。"

---

#### 项目6：闲鱼自动客服

**背景：** 闲鱼买家消息多，人工回复不过来

**挑战：** 闲鱼没有官方 API，怎么接入？

**方案：**
- Node.js 通过逆向接口监听消息
- LLM 做意图识别，分类售前问题
- 多轮对话保持上下文

**结果：** 秒级响应，减少人工介入

**反思：**
> "这个项目让我理解了'信息差'的价值——大多数人不会用 AI，帮他们用就是服务。
> 虽然现在不打算继续做，但这个经验让我对 AI 服务的商业落地有了体感。"

---

#### 项目7：客服引擎 v1.0 — AI 对话系统

**背景：** 简历中缺少一个专门展示"AI 对话能力"的项目，企业面试高频考察意图识别、多轮对话、知识库融合。

**挑战：** 如何在没有真实平台接入的情况下，展示完整的对话工程能力？

**方案：**
- 多级意图识别：LLM 为主 + 关键词兜底，置信度低时自动降级
- 多轮对话状态机：Session 级上下文管理，历史窗口可配置
- RAG 知识库融合：倒排索引 + TF-IDF，检索结果拼入 prompt
- 工程规范补齐：pytest 18个测试 + Docker + GitHub Actions CI

**结果：** 6个 API 端点，浏览器演示页面，18/18 测试通过，完整工程规范

**反思：**
> "这个项目最大的价值不是代码本身，而是补齐了简历中'AI 对话'能力的展示缺口。
> 面试官问到对话系统时，我可以讲清楚意图识别的多级策略、会话上下文的管理方式、
> RAG 检索如何减少幻觉。同时 pytest + Docker + CI 也回应了企业对工程规范的要求。"

---

## 三、高频技术问题 & 答案要点

### MCP 相关

**Q: MCP 是什么？解决了什么问题？**
> Model Context Protocol。大模型缺少对外的"手"——它只能生成文本，
> 不能直接操作工具。MCP 定义了标准化的工具描述和执行协议，
> 让 LLM 能通过统一接口调用任意工具，就像给 AI 装上了通用插座。

**Q: MCP 的 Tools / Resources / Prompts 有什么区别？**
- **Tools**：LLM 可以调用的函数（有输入输出）
- **Resources**：LLM 可以读取但不可修改的数据
- **Prompts**：预定义的对话模板，降低提示词工程成本

**Q: 你在项目中怎么用 MCP 的？**
> 接入了 Playwright MCP（浏览器自动化）和 Native DevTools MCP（桌面操作），
> 让 Claude Code 能直接操控浏览器和桌面。也在设计自己的 leaf-tian 协议
> 作为 MCP 思想的延伸。

---

### Agent 相关

**Q: 多 Agent 和单 Agent 的区别是什么？**
> 单 Agent 把所有任务塞进一个上下文窗口，长对话会衰减。
> 多 Agent 按领域拆分，每个专家专注自己的范围，
> 通过协议通信，上下文更小、更专注、质量更稳定。

**Q: 你遇到过的最大技术挑战是什么？**
> cc-connect 的飞书凭证竞态问题——两个进程同时用同一个 token，
> 互相踢下线。最后用 Go 做单实例网关 + session key 隔离解决了。
> 这个问题的价值在于让我理解了"状态管理"在分布式系统里的重要性。

---

### 工程相关

**Q: 你怎么保证系统的可靠性？**
> 三层：guardian 自动重启 + dispatcher 重试补发 + watchdog 边界监控。
> 核心思想是"接受故障必然发生，设计系统让它能自愈"。

**Q: 没有正式工作经验，怎么证明你能胜任？**
> 我的 Jarvis 生态就是一个完整的工业级系统——有架构文档、有协议规范、
> 有服务治理、有监控告警。这不是 demo，是跑在服务器上 24/7 的真实系统。
> 你可以在 GitHub 上看到完整代码和文档。

---

### RAG 相关（新增项目）

**Q: 你的 RAG 系统用了什么技术方案？**
> 生产级本地 RAG 引擎，混合检索（关键词 + TF-IDF向量）。
> ChromaDB 存储向量，自实现倒排索引 + TF-IDF 计算，
> 零外部依赖，完全离线运行。检索延迟 <50ms，592个知识块。

**Q: 为什么用 TF-IDF 而不是 Embedding 模型？**
> 三个原因：一、HuggingFace 被墙无法下载模型；二、TF-IDF 速度更快（<1ms vs 50ms+）；
> 三、对中文专业术语（MCP、Jarvis、飞书）关键词匹配反而比语义检索更精确。
> 这是工程上的务实选择。

**Q: 混合检索怎么融合排序？**
> 关键词召回占35%权重（BM25变体），向量召回占65%权重（cosine相似度）。
> 这样专业术语精确匹配时关键词得分高，模糊语义查询时向量得分高。
> 可以按需调整比例。

**Q: 索引持久化怎么设计的？**
> 三层持久化：ChromaDB存向量数据，JSON文件缓存倒排索引和chunk元数据。
> 重启加载 <100ms。重建索引一键命令，自动重新扫描所有Markdown文档。

**Q: 支持流式输出吗？**
> 支持。提供 SSE 接口 /stream，逐结果流式返回，前端可以边检索边显示。
> 适合生产环境的用户体验。

---

## 四、投递渠道清单

| 平台 | 状态 | 建议操作 |
|:-----|:----:|:---------|
| Boss直聘 | 待投 | 搜"AI应用工程师""Agent开发""大模型应用" |
| 拉勾网 | 待投 | 同上，补充投递 |
| 脉脉 | 待投 | 找内推，比海投效率高 3x |
| 智联招聘 | 待投 | 备用渠道 |
| 实习僧 | 待投 | 如果考虑实习转正 |
| 学校就业网 | ✅ | 信阳职院应该有校招信息 |
| GitHub | ✅ | README 已优化，等猎头 |

**搜索关键词：**
- AI应用工程师
- Agent开发工程师
- LLM应用开发
- 大模型应用工程师
- AI自动化工程师
- MCP开发

---

## 五、本周行动计划

| 日期 | 任务 | 完成标准 |
|:-----|:-----|:--------|
| 周二 | git push 推到 GitHub + 注册 Boss直聘 | 简历在线可见 |
| 周三 | 投 10 个岗位 + 准备项目1讲解 | 10个投递记录 |
| 周四 | 投 10 个岗位 + 准备项目2讲解 | 累计20个投递 |
| 周五 | 投 10 个岗位 + 练习自我介绍 | 累计30个投递 |
| 周六 | 查漏补缺 + 准备技术面试 | 能讲完全部6个项目 |
| 周日 | 复盘 + 调整简历关键词 | 根据反馈迭代 |

**每天记住：**
- 投完一个岗位 → 记录岗位名+公司+链接
- 收到面试 → 立刻复习对应项目的讲解
- 被拒 → 问原因，改简历，继续投

---

*最后更新：2026-08-31*
